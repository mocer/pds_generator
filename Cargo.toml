[package]
name = "pds-generator"
version = "0.1.0"
edition = "2024"

[dependencies]
clap = { version = "4.6.0", default-features = false, features = [
  "derive",
  "std",
] }

[[bin]]
name = "pds-generator"
path = "main.rs"
